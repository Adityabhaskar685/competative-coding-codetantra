import sys

arr = [int(x) for x in sys.argv[1].split(',')]

def middleElementOfArray(arr):
	
	if len(arr)%2==0:
		mid=len(arr)//2
		return arr[mid-1]
	else:
		mid= len(arr)//2
		return arr[mid]


print(middleElementOfArray(arr))
